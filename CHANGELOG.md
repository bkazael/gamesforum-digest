# CHANGELOG - תיעוד שינויים וארכיטקטורה

## [2026-08-24] - הגדרת סביבת בדיקות בטוחה (CI) ב-GitHub Actions

### 1. מה עשינו? (What)
* **הקמת Workflow לבדיקות יבשות:** נוצר קובץ `.github/workflows/test.yml` המריץ את בדיקות הצינור היבשות בלבד (`test_pipeline.py`).
* **הפרדה מוחלטת מסביבת הפרודקשן:** תהליך הבדיקות אינו נוגע ב-`feed.xml`, אינו משנה את `memory.json`, ואינו מפיק קבצי אודיו אמיתיים[cite: 9, 10, 11].
* **אפס ניצול טוקנים (Zero-Token Testing):** הרצת ה-CI משתמשת ב-Mocking מלא עבור מודלי ה-LLM וה-TTS.

### 2. למה עשינו את זה? (Why)
* **בטיחות בפרודקשן:** מניעת הזרמת פרקי דמה/ניסיון למאזינים הרשומים לפודקאסט ב-Feed החי.
* **אימות רציף וחיסכון בעלויות:** אפשרות לבדוק תקינות קוד מלאה בתוך 10 שניות ישירות ב-GitHub ללא עלות טוקנים וללא צורך בהרצה מקומית במחשב.

### 3. איך מריצים בדיקה בטוחה ב-GitHub?
1. נכנסים ל-Tab של **Actions** ב-GitHub.
2. בוחרים בצד שמאל ב-Workflow **CI Test Suite (Dry-Run Only)**.
3. לוחצים על **Run workflow**.

---

## [2026-08-24] - אכלוס הזיכרון ההיסטורי (Episodes Memory Population)

### 1. מה עשינו? (What)
* **עדכון קובץ הזיכרון (`memory.json`):** הוזנו הנתונים והתקצירים מ-3 הפרקים הקודמים של הפודקאסט[cite: 9, 10, 11].
* **מבנה הנתונים בזיכרון:** לכל פרק הוגדרו נושאים מרכזיים (`topics_covered`) ותובנות/עמדות מפתח (`key_takeaways`)[cite: 9, 10, 11].

### 2. למה עשינו את זה? (Why)
* **אחזור נרטיבי רציף:** כעת Gemini יוכל לשלוף ישירות מקובץ זה התפתחויות מתמשכות (כמו המעבר ל-D2C/Webstores[cite: 9, 10, 11], המאבק של אפל באירופה[cite: 9, 11], או הסטטוס של Supercell Mo.co[cite: 9, 10]) ולשלב אותם בתסריט השיחה של דנה ויוני.
* **יעילות בטוקנים:** במקום לשמור את כל מכלול הטקסט/תסריטים של הפרקים, התקציר המובנה צורך כמות טוקנים אפסית בשלב הדירוג והתסריט.

### 3. איך לבדוק שזה עובד?
מריצים את סקריפט הבדיקות היבש:
```bash
python test_pipeline.py
