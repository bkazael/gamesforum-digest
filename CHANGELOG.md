# CHANGELOG - תיעוד שינויים וארכיטקטורה

## [2026-09-23] - מקור מסוג email: קליטת Gamigion בלי לגעת באתר החסום

### 1. מה עשינו? (What)
* מתאם חדש `sources.py::from_email()` (`kind = "email"`): קורא ב-IMAP
  מתיבת דואר ייעודית את מיילי "פוסט חדש" של Substack, שולף מהם כותרת +
  קישור קנוני + **גוף הכתבה המלא** ישירות מה-HTML של המייל.
* `discovery.py` שלב 3: אם ל-candidate כבר יש `text` (כמו שמייל-מקור
  מספק), הוא מדלג על `fetch_article(cand["url"])` לגמרי — אין שום קריאת
  HTTP למקור החסום, לא בשלב האיסוף ולא בשלב ההעשרה.
* `sources.collect()`: כל שגיאת אדפטר נתפסת בנפרד ולא מפילה ריצה שלמה;
  `raw_item_counts` מקבל `-1` בתור סימן מובחן ל"כשל ממש" (למשל כניסה
  ל-IMAP נכשלה), נבדל מ-`0` שהוא "נשלף בהצלחה, כלום חדש".
* `source_health.check()`: `kind = "email"` עם 0 פריטים = שקט תקין, אין
  התרעה. `-1` כן מתריע, בכל קטגוריה, בניסוח שאומר "השליפה עצמה נכשלה"
  ולא "0 תוצאות".
* `weekly-digest.yml` מעביר `GMAIL_ADDRESS`/`GMAIL_APP_PASSWORD` ל-pipeline
  (ריק/לא בשימוש כל עוד אין מקור email מוגדר בפועל). `profile.toml` מכיל
  את קונפיג Gamigion המלא, מושבת בהערה, עם הוראות הפעלה מדויקות.
* בדיקות חדשות: `test_email_source.py` (12 בדיקות, IMAP מדומה לגמרי —
  הודעה תקינה, תיבה ריקה, הודעה בלי קישור, כשל כניסה, קרדנציאלס חסרים,
  ו-`collect()` ששורד אדפטר שנכשל) ותוספות ל-`test_contracts.py`
  ו-`test_source_health.py` לבדוק את שני צדי החיווט.

### 2. למה עשינו את זה? (Why)
* גילינו שהרעיון הקודם ("נוסיף RSS מדומה מאימייל") לא היה עובד: `discovery.py`
  תמיד שולף מחדש מה-URL בשלב 3, אז גם item שמגיע ממייל היה נתקל שוב
  ב-403 של Cloudflare רגע אחר כך. הפתרון האמיתי הוא לצאת מהזירה של
  Substack לגמרי — מייל של פוסט חדש כבר מכיל את הכתבה המלאה, אז אין
  שום צורך לגעת בשרת של Substack בכלל.
* ניוזלטר לא יוצא כל שבוע. בלי הפטור המפורש ל-`kind = "email"`,
  `source_health.py` היה מתריע כל שבוע שקט — בדיוק הדפוס שגרם להשבתת
  הניסיון הקודם עם Gamigion (ר' 2026-09-22): מייל כשלון קבוע על מצב
  ידוע ותקין מלמד להתעלם מההתרעה האמיתית הבאה.

### 3. מה עדיין פתוח, ומה תלוי בך
* **הפרסינג לא אומת מול מייל אמיתי.** `_FOOTER_MARKERS` ב-`sources.py`
  נבנה נגד תבנית סינתטית שמדמה מייל Substack, לא נגד מייל אמיתי
  מ-Gamigion — אין דרך מהסביבה הזו לקבל אחד. הוראות הכיוונון (מה לשלוח
  לי אם זה לא עובד בפעם הראשונה) נמצאות בהערה מעל קונפיג Gamigion
  ב-`profile.toml`.
* **שלושה צעדים כדי להפעיל בפועל**, מפורטים באותה הערה: Gmail ייעודי
  + מינוי לניוזלטר, App Password (דורש אימות דו-שלבי), ושני secrets
  חדשים ב-repo. עד אז המקור נשאר מושבת ואין שינוי בהתנהגות הקיימת.
* **`gamigion.com/journal` (האתר, לא הניוזלטר) עדיין פתוח בנפרד** —
  יכול להיות שהוא לא חסום בכלל (דומיין שונה מ-Substack), אבל דורש
  `link_pattern` אמיתי כדי לבנות לו מתאם HTML רגיל כמו Gamesforum.
  עדיין ממתין לדוגמאות `<a href>` אמיתיות מה-view-source של העמוד.

### 4. איך בודקים?
```
python test_email_source.py && python test_contracts.py && python test_source_health.py
```
כל שינויי הקוד תלויים ב-mock בלבד; שום credential אמיתי לא נדרש כדי
להריץ את הבדיקות. אין שינוי בהתנהגות היום — המקור נשאר מושבת עד
שהתשתית (Gmail + secrets) קיימת בפועל.

---

## [2026-09-22d] - שני דפוסים חוזרים: פתיח מוכתב וגלישת שם ספק מהזיכרון

### 1. מה עשינו? (What)
* הוסר משפט הפתיחה המילולי של יוני ("היי דנה, שבוע מרתק בתעשייה") מהפרומפט,
  והוחלף בהוראה שדורשת תגובה ספציפית למשהו אמיתי מכתבות השבוע — ואוסרת
  במפורש משפט מצב-רוח גנרי שיכול לפתוח כל שבוע.
* בלוק `PREVIOUS EPISODES` בפרומפט התסריט קיבל הגבלה: אסור להזכיר שם חברה/
  ספק מפרק קודם אלא אם יש עליו כתבה **החדשה** ברשימת `SOURCE ARTICLES` של
  השבוע הזה. המשכיות מותרת ברמת נושא ("מגמת ה-D2C ממשיכה"), לא ברמת שם ספק.
* הוראת ה-scoring ל-vendor bylines (`discovery.py`) חודדה: נתונים אמיתיים
  בכתבה כבר לא "מכשירים" אותה אוטומטית אם המסקנה שלה היא "תשתמשו בפתרון
  כמו שלנו" — צריך לשפוט את הטיעון, לא רק אם יש בו מספר.

### 2. למה עשינו את זה? (Why)
* המשתמש שם לב שיוני פותח **כל שבוע** באותו משפט. בדיקה מול שמונה תסריטים
  (17.8 עד 22.9) אישרה: זה לא טיק של המודל — המשפט מוכתב מילולית בקוד,
  אז Gemini פשוט מצטט אותו כל שבוע.
* המשתמש גם חשד שהמנחים "פרסמו" את Xsolla יותר מדי. בדיקה בפרק של 15.9
  מצאה הוכחה חדה: `episodes/2026-09-15.json` **לא מכיל אף כתבה על Xsolla**
  באותו שבוע, ובכל זאת יוני אומר בתסריט "בפרקים קודמים דיברנו על Xsolla
  ו-ZBD" — שם הספק מגיע מ-`memory.py` בלבד, לא מכתבה שנבחרה השבוע. זו
  בדיוק ה"תוכן שיווקי נסתר" שהמשתמש חשד בו: מותג חוזר על עצמו גם בלי
  שום עדכון עדכני עליו.
* בפרק של 22.9 הפתיחה כולה נבנתה סביב טור של מנהל ב-Xsolla, וההוראה
  הקיימת ל-scorer ("unless the data stands on its own") נתנה לזה מעבר
  חופשי כי הטור כלל נתונים אמיתיים. זו הפרצה שסגרנו.

### 3. איך בודקים?
```
python test_episode.py && python test_memory.py && python test_discovery.py && python test_contracts.py && python test_source_health.py && python test_checkpoint.py
```
שינויי טקסט פרומפט בלבד — אין שינוי סכמה או חיווט, כל שש החבילות עוברות
ללא שינוי. **אי אפשר לאמת מקומית שהניסוח החדש באמת משנה את הפלט** — זו
בדיקה שדורשת קריאת Gemini אמיתית; תבחן את זה בפרק הבא (29.9) מול הבעיות
הספציפיות האלה.

---

## [2026-09-22c] - שם התוכנית הופיע פעמיים בכותרת הפרק

### 1. מה עשינו? (What)
* `show_title()` חדש ב-`gamesforum_pipeline.py` מוסיף את `Ben's Weekly
  Digest` לכותרת **בדיוק פעם אחת**: הוא מקלף קודם כל מופע קיים בסוף
  הכותרת ורק אז מוסיף. קודם ההוספה היתה ללא תנאי.
* תוקנה גם הכותרת של הפרק שכבר פורסם (15.9) ב-`episodes/2026-09-15.json`,
  ו-`feed.xml` נבנה מחדש.

### 2. למה עשינו את זה? (Why)
* הפרק של 15.9 יצא ל-feed החי ככה: `... | Ben's Weekly Digest | Ben's
  Weekly Digest`. הסיבה: הפרומפט של התסריט מראה למנחים להגיד את שם
  התוכנית בקול, אז Gemini לפעמים מכניס אותו גם ל-`episode_title` עצמו,
  והקוד הוסיף אותו שוב על גבי זה.
* זה נראה קוסמטי אבל זה מה שהמאזין בפועל **קורא** באפליקציה, ו-guid של
  פריט שפורסם הוא לצמיתות — הכותרת היא בדיוק החלק שעוד אפשר לתקן אחרי
  שפרק כבר יצא. התיקון ב-JSON בטוח בדיוק מהסיבה הזו: ה-guid לא זז,
  והפלייר פשוט יציג את הכותרת המתוקנת.
* התיקון אידמפוטנטי בכוונה (לולאה, לא `if` יחיד), כדי שגם כותרת שחוזרת
  עם השם פעמיים תתנקה.

### 3. איך בודקים?
```
python test_contracts.py
```
בדיקה 8 מריצה את `show_title()` על שש כותרות אמיתיות: בלי שם, עם שם,
עם שם כפול, רווחים מיותרים, ורק-השם.

---

## [2026-09-22b] - ריצה חוזרת אוטומטית: יום גרוע של Gemini מתקן את עצמו

### 1. מה עשינו? (What)
* `weekly-digest.yml` רץ עכשיו **שלוש פעמים** בשני: 03:00, 06:00 ו-10:00
  UTC (במקום פעם אחת).
* `main()` קיבל שומר אידמפוטנטיות: אם `episodes/<today>.mp3` כבר קיים,
  הריצה יוצאת מיד עם 0 בלי לגעת בכלום. `FORCE_REGENERATE=1` עוקף אותו
  במכוון (ככה שוחזר הפרק הפגום של 31.8).

### 2. למה עשינו את זה? (Why)
* שתי התקלות של ספטמבר (14.9 ו-21.9) היו שתיהן **איטיות רגעית של Gemini**,
  לא באג אצלנו — ושתיהן דרשו שמישהו ישים לב ויריץ מחדש ידנית. התיקונים
  הקודמים הפכו כישלון לזול (checkpoint) אבל לא הפכו אותו לנדיר: אם TTS
  ייתקע שוב בשני הבא, הריצה עדיין תיכשל, פשוט בחן. ריצה חוזרת אוטומטית
  היא מה שסוגר את הפער בין "נכשל בצורה מסודרת" ל"בסוף יצא פרק".
* השילוב הוא מה שהופך את זה לזול באמת: בזכות ה-checkpoint, ריצה חוזרת
  אחרי כישלון חלקי מסנתזת רק את ה-chunks שחסרים — דקות ספורות וכמעט אפס
  מכסה. בלי השומר, לעומת זאת, שלוש ריצות בשני היו מייצרות שלושה פרקים.
* הכיוון המסוכן של השומר הוא דווקא ההפוך: אם הוא יופעל בטעות, **שום פרק
  לא יתפרסם יותר** והכל עדיין ידווח הצלחה. לכן הוא נבדק בשני הכיוונים.

### 3. איך בודקים?
```
python test_contracts.py
```
שלוש בדיקות חדשות: ריצה אחרי פרק שכבר פורסם יוצאת 0 **ולא** מריצה
discovery מחדש; `FORCE_REGENERATE=1` כן מריץ; וכשאין פרק להיום השומר לא
חוסם כלום.

---

## [2026-09-22] - תקציב זמן קוהרנטי, ו-checkpoint שמונע אובדן ריצה שלמה

### 1. מה עשינו? (What)

**הרקע:** ריצת 21.9 (שבועית, מתוזמנת) נהרגה ע"י ה-timeout של 30 דקות ברמת
השלב. הפרק לא יצא. מהלוג עולה התמונה המלאה: chunk 1 של ה-TTS בזבז לבדו
**20 דקות** — ארבעה נסיונות שכל אחד מהם נתקע עד תום ה-timeout של 300
שניות, עם sleeps שמטפסים ביניהם — ואז chunk 2 הצליח, ו-chunk 3 נהרג
באמצע. שני chunks מוכנים, תסריט מלא של 1915 מילים, וכל מכסת הדירוג —
הכל נזרק.

**תיקון 1 — התקציב לא היה קוהרנטי בכלל.** `RUN_DEADLINE_SEC` היה 2400
שניות (40 דקות) בעוד השלב ב-CI נחתך אחרי 30. כלומר `_check_deadline()`,
המנגנון שאמור לעצור אותנו בחן לפני שההוצאה-להורג מגיעה, **מעולם לא יכול
היה לרוץ ראשון** — קוד מת. עכשיו השכבות מקוננות כמו שצריך:
נסיון בודד (`TTS_TIMEOUT` = 270 שניות) < הריצה (`RUN_DEADLINE_SEC` =
25 דקות) < השלב (30) < ה-job (35). בנוסף: `tries` ב-TTS ירד מ-8 ל-4
(ב-8 נסיונות × 300 שניות + sleeps מטפסים, chunk בודד יכול היה לבזבז ~52
דקות — יותר מכל תקציב ה-job, בשביל רבע מהאודיו של פרק אחד), וה-sleeps
נחסמו ב-60 שניות במקום לטפס ל-175.

**תיקון 2 — `checkpoint.py` חדש: ריצה שנכשלת כבר לא מאבדת הכל.** כל שלב
יקר נשמר לדיסק ברגע שהוא מצליח, במפתח לפי תאריך הפרק:
* אחרי `generate_podcast_content()` — הכתבות הנבחרות + התסריט. זה אומר
  שכל קריאות הטקסט ל-Gemini (batches של דירוג, בדיקות כפילות, יצירת
  התסריט) כבר שולמו, וריצה חוזרת **לא משלמת עליהן שוב**.
* כל chunk אודיו נשמר ברגע שהוא חוזר, לפי המיקום שלו.
ריצה חוזרת לאותו תאריך מתחילה ממה שכבר קיים ומסנתזת רק את מה שחסר. ריצה
שמסתיימת בהצלחה מוחקת את ה-checkpoint (רק פרק שפורסם באמת "הרוויח" את
הזכות למחוק אותו).
* ב-CI כל ריצה מקבלת runner נקי, אז ה-checkpoint לבדו היה נעלם בין
  נסיונות — נוסף `actions/cache` ב-`weekly-digest.yml` עם
  `if: always()` בשמירה (ברירת המחדל של cache היא לשמור רק בהצלחה, כלומר
  בדיוק להפך ממה שצריך כאן).

זה מטפל גם בתקלה של 14-15.9: שם שתי ריצות מלאות על אותו פרק גמרו את מכסת
הבקשות היומית של הטייר החינמי. עם checkpoint, ריצה חוזרת עולה כמעט כלום.

**תיקון 3 — Gamigion הושבת.** ה-403 של Cloudflare אומת פעמיים ושרד גם את
מעבר ה-User-Agent, כלומר חסימה ברמת IP/TLS שלא ניתנת לעקיפה מ-CI. הוא
הפיק התרעה זהה בכל ריצה — מייל כשלון שבועי קבוע על משהו שאי אפשר לפעול
לגביו, שזו בדיוק הדרך לאמן מישהו להתעלם מההתרעה הבאה, שאולי כן תהיה
אמיתית. הקונפיג נשמר כהערה ב-`profile.toml` ומוכן לחזור ברגע שקליטת
המייל תעבוד.

### 2. למה עשינו את זה? (Why)
* הגדלת ה-timeout (20→35 ב-14.9) טיפלה בסימפטום ולא בסיבה: כל עוד לולאת
  הנסיונות יכולה לבקש יותר זמן מכל תקציב שנקצוב לה, כל תקרה תיפרץ בסוף.
  הקינון של התקציבים הוא התיקון האמיתי.
* אבל גם תקציב מושלם לא עוזר אם כישלון שורף הכל: הבעיה האמיתית היתה
  שתקלה רגעית אחת ב-API עלתה פרק שלם **ועוד** נתח ממכסת היום. checkpoint
  הופך כישלון כזה מ"אבד שבוע" ל"אבדו כמה דקות".

### 3. איך בודקים?
```
python test_checkpoint.py && python test_episode.py && python test_contracts.py
```
* `test_checkpoint.py` (חדש): round-trip, בידוד בין תאריכים, chunk ריק
  שנחשב חסר, קובץ פגום שנקרא כ"אין checkpoint" במקום לקרוס, ו-`clear()`.
* `test_episode.py`: בדיקה חדשה שסופרת קריאות API ומוודאת ש-chunk
  שנשמר **באמת לא מסונתז שוב** — הכישלון כאן שקט (הפרק עדיין יוצא תקין,
  רק משלם פעמיים), אז רק ספירת קריאות תופסת אותו.
* `test_contracts.py`: מריץ את `main()` עם checkpoint קיים כש-`gemini_json`
  ו-`discovery.select` מוחלפים בפונקציות שזורקות חריגה — כלומר הבדיקה
  נכשלת אם ריצה משוחזרת נוגעת ב-Gemini בכלל.

---

## [2026-09-15] - הפרק כן יצא, אבל ההתרעה השקרית מהערך הקודם התגלתה

### 1. מה עשינו? (What)
* ריצה ידנית ב-15.9 הצליחה במלואה — הפרק פורסם (`7a522c8`, 35 turns,
  1884 מילים) — אבל `"Report pipeline failure"` (שנוסף אתמול) **רץ בכל
  זאת** והדפיס "no episode was published", למרות שהפרק כן פורסם. הסיבה:
  `if: failure()` בודק אם **כל** שלב קודם נכשל, לא ספציפית "Run pipeline"
  או "Commit episode" — ו-"Check source health" נכשל (כמו שצריך, על
  Gamigion) וגרר את השלב הזה איתו.
* התיקון: `"Run pipeline"` ו-`"Commit episode"` קיבלו `id:`, וה-`if:`
  של "Report pipeline failure" עכשיו בודק במפורש
  `steps.run_pipeline.outcome == 'failure' || steps.commit_episode.outcome == 'failure'`
  (עם `failure() &&` בהתחלה — בלי זה, ה-`if:` המפורש נעטף אוטומטית
  ב-`success()` ולעולם לא ירוץ אחרי כישלון, זה gotcha ידוע ב-GitHub
  Actions). עכשיו השלב הזה רץ **רק** כשבאמת לא יצא פרק.
* **עדיין פתוח:** feed של Gamigion עדיין מחזיר 403 גם עם UA של דפדפן
  אמיתי (מהתיקון של 14.9) — כנראה חסימת בוט מתוחכמת יותר מ-Substack
  (Cloudflare / טביעת אצבע TLS / אתגר JS) שלא ניתן לעקוף עם urllib רגיל.

### 2. למה עשינו את זה? (Why)
* מייל התרעה שקרי ("לא יצא פרק" כשבפועל כן יצא) הוא בדיוק מה שגורם
  להתעלם מהתרעות עתידיות אמיתיות — "כרי הזאב". השכבה הזו קיימת בדיוק כדי
  שאפשר יהיה לסמוך עליה, אז דיוק כאן קריטי יותר מכל שכבת קוד אחרת בפרויקט.

### 3. איך בודקים?
בדיקה ידנית של YAML (`yaml.safe_load`) + כל חמשת קבצי הטסט הרגילים. האימות
האמיתי כבר קרה בפועל: ריצה עם "Check source health" נכשל ו-"Report
pipeline failure" לא רץ יהיה ההוכחה שהתיקון עובד.

---

## [2026-09-14] - סוגרים את החור: התרעה גם כשכל הריצה נכשלת/נחתכת

### 1. מה עשינו? (What)
* סוגר את ה"עדיין פתוח" מהערך הקודם באותו יום: עכשיו יש timeout **ברמת
  השלב** "Run pipeline" (30 דקות) שקטן מ-timeout של ה-job כולו (35).
  ההבדל קריטי: timeout ברמת job הורג את **כל** מה שנשאר בבת אחת (כולל
  שלבים שעוד לא התחילו); timeout ברמת שלב מכשיל רק אותו, ומשאיר ~5 דקות
  ל-job להמשיך לשלב הבא.
* נוסף שלב אחרון חדש, "Report pipeline failure", עם `if: failure()` —
  רץ **רק** אם "Run pipeline" (או "Commit episode") נכשל מכל סיבה שהיא:
  timeout, קריסה, שגיאת git push. הוא מדפיס `::error::` ברור ("no episode
  was published") ומכשיל את עצמו בכוונה.
* התוצאה: **כל** תרחיש כישלון עכשיו מגיע ל-conclusion `failure` של ה-job
  — או ש-"Check source health" תופס בעיה בתוכן (מקור ריק) אחרי פרסום
  מוצלח, או ש-"Report pipeline failure" תופס כישלון/timeout של הפייפליין
  עצמו לפני שהספיק לפרסם. שני המסלולים שולחים את אותו מייל ברירת מחדל
  של GitHub — עדיין בלי שום secret או שירות חדש, בדיוק כמו שביקשת.

### 2. למה עשינו את זה? (Why)
* בעל הפרופיל ביקש במפורש: התרעות במייל על **כל דבר**, לא רק על מקור
  שהחזיר ריק. הפער שנשאר פתוח מהערך הקודם היה בדיוק התרחיש שקרה בפועל
  באותו שבוע — ולכן הוא הראשון שראוי לסגור, לא רק לתעד.
* `failure()` (לא `cancelled()`) הוא הביטוי הנכון כאן כי timeout ברמת
  שלב מסתכם ב-conclusion "failure" רגיל, לא "cancelled" — `cancelled()`
  שמור למקרה שה-run כולו בוטל, שהעיצוב הזה בדיוק נועד למנוע מלהגיע אליו.

### 3. איך בודקים?
בדיקה ידנית: YAML אומת עם `python3 -c "import yaml; yaml.safe_load(...)"`.
אין דרך זולה לדמות timeout אמיתי של 30 דקות ב-CI מדומה, אז האימות האמיתי
יגיע רק אם/כשתרחיש דומה יקרה שוב — ואז ה-job אמור לצאת אדום עם הודעה
ברורה, לא שקט.

---

## [2026-09-14] - הריצה השבועית נחתכה ב-timeout: אין פרק, אין אפילו התרעה

### 1. מה עשינו? (What)
* **מה קרה בפועל:** ריצת 14.9 נחסמה ע"י `timeout-minutes: 20` של ה-job,
  בדיוק ב-20:13 דקות. שלוש קריאות TTS לקחו 3-7 דקות **כל אחת** (כולל
  HTTP 500 אמיתי אחת מהן), ועוד כ-3 דקות של ניסיונות חוזרים על שגיאות 503
  בשלב הדירוג — לא באג, פשוט יום איטי/לא יציב אצל Gemini. ה-job נהרג
  20 שניות לתוך ה-chunk האחרון (הכי קטן, 834 תווים) — שנייה לפני שהיה
  מסיים.
* **הבעיה השנייה, האמיתית:** כש-`timeout-minutes` ברמת ה-job נחתך, **כל**
  השלבים שנשארו — כולל "Commit episode" וגם "Check source health" —
  קופצים ל-`skipped` באופן גורף. כלומר: לא רק שלא יצא פרק, גם שכבת
  ההתרעה שהוספנו לפני כמה ימים **לא קיבלה בכלל הזדמנות לרוץ**. ה-log
  בפועל כן תפס תקלה אמיתית ונכונה (`Gamigion (Mobile Gaming Today):
  feed unreachable: HTTP Error 403`, עם האבחון הנכון "ספציפי למקור הזה")
  — אבל אף אחד לא ראה את זה כי הריצה כולה נחתכה לפני שהגיעה לשלב שמדווח.
* שני תיקונים:
  1. `timeout-minutes` עלה מ-20 ל-35 — מרווח אמיתי מול השונות בזמני
     תגובה של Gemini, לא רק "התאמה בדיעבד למספר שראינו".
  2. `UA` ב-`gamesforum_pipeline.py` שונה ממחרוזת שמזהה את עצמה כ-bot
     (`"compatible; bens-digest/6.3"`) ל-User-Agent של דפדפן אמיתי —
     ה-403 מ-Substack קרה **רק** למקור הזה, כששאר המקורות (עם אותו UA)
     עבדו תקין, מה שמצביע על חסימת bot בסיסית בצד Substack.

### 2. למה עשינו את זה? (Why)
* 20 דקות היה מספיק בכל ריצה קודמת, אבל בלי שום מרווח אמיתי מול יום איטי
  של ה-API — וזה בדיוק מה שקרה. 35 דקות נותן מרווח אמיתי בלי לשנות שום
  התנהגות של הקוד עצמו.
* גילינו חור אמיתי בעיצוב שכבת ההתרעה מלפני כמה ימים: היא בנויה בהנחה
  שה-pipeline **מסיים לרוץ** (בהצלחה או בכישלון רגיל), אבל לא מכסה מצב
  שבו כל ה-job נחתך שרירותית ע"י timeout. זו לא בעיה שתוקנה ישירות (אין
  דרך אמינה להריץ שלב "cleanup" אחרי timeout ברמת job ב-GitHub Actions
  בלי לפצל timeout ברמת שלב בנפרד) — אבל **תיקון ה-timeout עצמו** מקטין
  דרמטית את הסיכוי שהתרחיש הזה יקרה שוב.
* ה-UA שונה כי זו הסיבה הכי סבירה וזולה-לתקן ל-403 שראינו בפועל, גם אם
  אין ערובה שזה יעבוד תמיד מול חסימות בוט מתוחכמות יותר.

### 3. איך בודקים?
בדיקה ידנית בלבד הפעם — אלה שינויי תצורה/רשת, לא לוגיקה שניתן למדל ב-
mock. `python test_episode.py && ... && python test_source_health.py`
כולם עדיין עוברים (שום דבר בממשק לא השתנה). האימות האמיתי: הריצה הידנית
הבאה של `weekly-digest.yml`, ובדיקה שהיא מסיימת בפחות מ-35 דקות ושפרק
14.9 שהוחמץ יוצא.

**עדיין פתוח:** אין עדיין דרך לקבל התרעה אם *כל* הריצה נחתכת ע"י timeout
(לא רק תוכן שחזר ריק). אם זה יקרה שוב, שווה לחשוב על timeout נפרד ברמת
השלב "Run pipeline" (קטן יותר מה-timeout של ה-job), כדי שיישאר זמן
לשלב "Check source health" לרוץ גם במקרה כזה.

---

## [2026-09-11] - התרעה מיידית במקום שבועיים, עם אבחון "זה אנחנו או הם"

### 1. מה עשינו? (What)
* `source_health.py` נכתב מחדש: **אין יותר סף של שתי ריצות רצופות**. עכשיו
  ריצה בודדת שבה מקור חוזר עם אפס תוצאות מספיקה כדי להתריע — מיד, בלי
  לחכות שבוע נוסף.
* נוסף אבחון: כשמקור נכשל, הפונקציה `check()` בודקת אם **שאר מקורות ה-RSS**
  באותה ריצה כן החזירו תוצאות. אם כן — ההתרעה מצביעה על המקור הספציפי
  (כנראה ה-feed שלו זז/נפל). אם **כל** מקורות ה-RSS נכשלו יחד — ההתרעה
  אומרת את זה במפורש: "כנראה זה אנחנו (רשת/קוד), לא כל האתרים שנפלו בו-זמנית".
* אין יותר `source_health.json` מתמיד עם היסטוריית ריצות — ההחלטה מחושבת
  כל פעם מחדש מנתוני הריצה הנוכחית בלבד. הקובץ היחיד שנכתב עכשיו
  (`source_alerts.json`) הוא scratch זמני לריצה אחת, לא מחובר ל-git
  (`.gitignore`), ומועבר בין השלב שמריץ את ה-pipeline לשלב "Check source
  health" באותו job בלבד.
* ההתרעה עצמה ממשיכה להישלח דרך המייל האוטומטי של GitHub על ריצה כושלת —
  אושר במפורש: לא מוסיפים שירות מייל/secret חדש.

### 2. למה עשינו את זה? (Why)
* בעל הפרופיל ביקש במפורש: לא לחכות לדפוס חוזר, ולבדוק במיידית אם התקלה
  ספציפית למקור אחד או משהו רחב יותר אצלנו — בדיוק ההיגיון שמונע לבזבז זמן
  על חיפוש "מה השתנה באתר X" כשבפועל כל ה-RSS נפל כי הרשת/הקוד שלנו נשבר.
* גרסה קודמת (שתי ריצות רצופות, נשמר ב-git) הייתה שוקלת אמינות מול רעש —
  אבל היא לא ניגשה לשאלה הרלוונטית באמת: "האם זה המקור הזה או אנחנו?" —
  וגם דרשה state מתמיד בין ריצות, שעכשיו התייתר לגמרי.

### 3. איך בודקים?
```
python test_source_health.py
```
מכסה: ריצה בריאה בלי התרעות, מקור בודד שנכשל בזמן ששאר ה-RSS תקין (מתריע
מיד, מצביע נכון), כל ה-RSS נכשל יחד (הודעה שאומרת "זה כנראה אנחנו"), מקור
HTML שנכשל (הודעה שמפנה ל-`link_pattern`, לא לניסוח ה-RSS), וקובץ alerts
פגום/חסר שלא מפיל את הריצה. אומת גם ידנית: `python discovery.py --dry-run`
מה-sandbox החסום הזה בפועל הפיק את הודעת "כל ה-RSS נפל יחד" בדיוק כמצופה.

---

## [2026-09-11] - מקור Gamigion, ושכבת התרעה כשמקור מפסיק לעבוד

### 1. מה עשינו? (What)
* נוסף מקור RSS חדש: **Gamigion — "Mobile Gaming Today"** (`https://gamigion.substack.com/feed`,
  feed סטנדרטי של Substack, בלי scraping). אושר במלואו ע"י בעל הפרופיל,
  כולל סדרת "Deconstruction" (ניתוח level design מבוסס נתונים) שאין לאף
  מקור אחר — הוצא רק תוכן כנסים/אירועים, כבר מסונן ע"י `[hard_block]`
  הגלובלי + רשימת `block` ספציפית למקור הזה כהגנה כפולה.
* נוסף מודול חדש, `source_health.py`: עוקב אחרי כמה ריצות רצופות מקור
  מסוים חוזר עם **אפס תוצאות גולמיות** (`sources.collect()` עכשיו מחזיר
  גם `raw_item_counts` לצורך זה). אחרי `FAILURE_THRESHOLD` (2) ריצות
  רצופות כאלה, `source_health.pending_alerts()` מחזיר את שם המקור.
* `weekly-digest.yml` מקבל שלב אחרון חדש, **אחרי** commit הפרק: קורא
  ל-`pending_alerts()` ונכשל בכוונה אם משהו חזר. כישלון בשלב הזה לא
  יכול למנוע פרסום — הפרק כבר נדחף בשלב הקודם — אבל הוא הופך את הריצה
  כולה לאדומה, מה ש-GitHub שולח עליו מייל אוטומטי לבעלים כברירת מחדל.
  אין שרות/סוד/תשתית חדשים — ניצול של מה ש-GitHub Actions כבר עושה.

### 2. למה עשינו את זה? (Why)
* מקור שקורס בשקט (שינוי מבנה עמוד, feed שנעלם) היה עד עכשיו מוריד
  אחוז נתונים בלי שאף אחד ישים לב — הלוג עצמו כבר תיעד את זה
  (`"WARNING: zero links..."`), אבל רק מי שקורא לוגים של Action רואה
  את זה. עכשיו זה בלתי אפשרי להתעלם ממנו.
* סף של ריצה בודדת היה מייצר false positive על שבוע שקט לגיטימי; שתי
  ריצות רצופות הוא הקו שבין "שבוע שקט" ל"זה באמת שבור".
* בעל הפרופיל ביקש ספציפית להיצמד לגישת ה-scraping העצמאי (כמו
  Gamesforum) על פני שירות scraping צד-שלישי (rss.app) בדיוק בגלל הסיכון
  הזה של תלות בלתי-נראית — השכבה הזו היא התשובה לחשש הזה: גם אם
  ה-scraping שלנו יישבר, נדע.

### 3. איך בודקים?
```
python test_source_health.py
python test_contracts.py
```
`test_source_health.py` מכסה: מקור יציב לא מתריע, שבוע שקט בודד לא
מתריע, שתי החטאות רצופות כן מתריעות, החלמה מנקה את ההתרעה, מקורות
נעקבים בנפרד, וקובץ פגום לא מפיל ריצה. `test_contracts.py` מוודא
ש-`source_health` נכנס לחוטים בלי לגעת בקובץ האמיתי.

**עדיין פתוח:** `gamigion.com/journal` (האתר, לא הניוזלטר) עדיין ללא
מקור — אין לו RSS מאומת, וכתיבת מתאם HTML דורשת דוגמאות אמיתיות של
קישורי כתבות מהעמוד (חסום מה-sandbox), לא ניחוש.

---

## [2026-09-10] - קטיעת אודיו באמצע משפט, וה-"-v2" הקבוע שהשתחל בטעות

### 1. מה עשינו? (What)
* **הבאג שדווח:** בפרק 2026-09-07 האודיו נקטע באמצע משפט, ואז נשמע ממשיך
  לדבר בלי לסיים את המשפט הקודם. הסיבה: `gemini_tts_chunk()` שלח כל chunk
  (עד 3800 תווים) בקריאה אחת ל-Gemini TTS, אבל **מעולם לא בדק את
  `finishReason`** בתשובה. כשה-audio של chunk מסוים פגע בתקרת האורך
  הפנימית של המודל (`finishReason != "STOP"`, למשל `MAX_TOKENS`), ה-API
  עדיין מחזיר `inlineData` חלקי — וה-קוד הישן קיבל את זה כאילו זה chunk
  שלם, בלי שום סימן שמשהו נקטע.
* `gemini_tts_chunk()` עכשיו בודק את `finishReason`: אם הוא לא `STOP`
  (או ריק), האודיו נזרק ומנוסה שוב, בדיוק כמו כל שגיאת API אחרת. אם זה
  ממשיך לקרות אחרי כל הניסיונות — מועלה `TTSTruncatedError` ייעודי במקום
  להחזיר בשקט אודיו פגום.
* `synthesize_audio()` מטפל ב-`TTSTruncatedError`: אם chunk ממשיך להיקטע,
  הוא מתפצל לשניים (לפי גבולות תור-דיבור) וכל חצי מסונתז בנפרד ומחובר —
  רקורסיבית, עד ל-turn בודד. רק אם *turn בודד* עדיין נקטע, הריצה נכשלת
  בקול רם במקום לפרסם פרק פגום.
* **תוקן תוך כדי עבודה על זה:** `main()` הכיל `today = dt.date.today().isoformat() + "-v2"`
  — שאריות מתיקון ה-guid החד-פעמי מ-2 בספטמבר (ר' הערך למטה). זה היה
  הופך כל פרק עתידי לשם "-v2" לצמיתות. תוקן חזרה לתאריך פשוט; ה-versioning
  (`-v2`/`-v3`...) נשאר מנגנון ידני לרגע שבו צריך guid טרי לתאריך שכבר
  פורסם, לא ברירת מחדל.

### 2. למה עשינו את זה? (Why)
* קטיעת אודיו באמצע משפט היא בדיוק סוג התקלה ש"נכשל בשקט" — אין שגיאה,
  אין לוג, רק פרק פגום שיוצא לאוויר. הבדיקה על `finishReason` הופכת את זה
  לכישלון גלוי (retry, ואז split, ואז שגיאה אמיתית) במקום תופעה שרק
  משתמשים שומעים.
* ה-`"-v2"` הקבוע לא היה נראה כמו באג בהרצה בודדת (הפרק פשוט נקרא
  "2026-09-07-v2" ועבד), אבל היה שובר את כל מוסכמת ה-versioning
  שנבנתה ב-2 בספטמבר — כל פרק עתידי היה מתנגש בשם קבוע במקום לקבל תאריך
  ייחודי משלו.

### 3. איך בודקים?
```
python test_episode.py
```
בדיקה חדשה מדמה תשובת Gemini TTS עם `finishReason: MAX_TOKENS` (בלי שום
קריאת רשת אמיתית) ומוודאת ש-`gemini_tts_chunk()` זורק `TTSTruncatedError`
במקום להחזיר את האודיו החלקי כאילו הוא שלם.

---

## [2026-09-02] - guid קבוע ל-RSS, וניקוי הקטלוג הציבורי

### 1. מה עשינו? (What)
* פרק ה-31.8 שנוצר מחדש (ר' הערך הבא, "איכלוס זיכרון היסטורי") נדחף
  בהתחלה תחת אותו שם קובץ/guid כמו הריצה השבורה המקורית
  (`bens-digest-2026-08-31-v2`). לפי מוסכמת RSS, `<guid>` הוא מזהה קבוע —
  כל פלייר פודקאסטים שכבר "ראה" אותו לא יציג אותו שוב כפרק חדש, גם אם
  התוכן שמאחוריו השתנה. זו טעות שלי; תוקנה בשני שלבים:
  1. שינוי שם ל-`-v3` (guid חדש) — commit `dbba503`.
  2. תוך כדי כך התגלה באג אמיתי נוסף: `build_feed()` חילץ את התאריך מתוך
     שם הקובץ עם `.replace("-v2", "")` קשיח — מול קובץ `-v3` זה no-op,
     `strptime()` נכשל, וה-`except ValueError: continue` השמיט את הפרק
     מה-feed **בשקט**, בלי שום לוג. תוקן עם regex כללי שמוציא תאריך מכל
     שם קובץ בלי תלות בסיומת + לוג מפורש כשמשהו לא תואם — commit `bdf6335`,
     עם טסט רגרסיה חדש ב-`test_episode.py`.
* גם אחרי זה המשתמש דיווח שהאפליקציה (Apple Podcasts) עדיין מסרבת לאפשר
  מעקב מחדש אחרי אותו URL, גם עם cache-busting query string. נבדק ונפסל:
  אורך ה-`<enclosure>` שמוצהר ב-feed.xml תואם *בדיוק* לגודל קובץ ה-mp3
  בפועל (`git ls-tree -l` מול הערך ב-XML) — זו לא בעיית תוכן פגום.
  המסקנה: זה חסימת cache ברמת האפליקציה/החשבון (iCloud sync), לא באג
  בקוד — אין דרך לתקן את זה מהצד שלנו.
* כדי לצמצם בלבול עד שהאפליקציה תתאושש: פרק ה-15.8 (שהיה גם הוא ניסיון
  שלא צלח כמו שרצינו) הוסר לגמרי מה-feed ומה-repo. פרק ה-31.8 קיבל guid
  טרי נוסף (`-v3` → `-v4`) כדי לוודא שהוא לעולם לא נצפה תחת אף מזהה קודם,
  כולל בכל ניסיון עקיפה שכבר בוצע.

### 2. למה עשינו את זה? (Why)
* מזהה guid שחוזר על עצמו הוא באג אמיתי שגורם לפרקים מתוקנים "להיעלם"
  אצל מאזינים קיימים לצמיתות, גם כשה-pipeline עצמו עובד מושלם.
  `build_feed()`'s hardcoded `-v2` string-strip היה יכול לשבור כל עדכון
  guid עתידי באותה צורה בדיוק — התיקון עם regex פותר את זה פעם אחת ולתמיד.
* הסרת פרק ה-15.8 והמעבר ל-`-v4` הם ניקוי קטלוג, לא תיקון קוד: המטרה היא
  קטלוג ציבורי חד-משמעי (3 פרקים, כל אחד עם guid ייחודי שלא נראה בעבר)
  בזמן שהבעיה באפליקציה של המשתמש נפתרת בנפרד.

### 3. איך בודקים?
```
python test_episode.py
```
כולל את טסט הרגרסיה של `build_feed()` שבונה feed מדומה עם `-v2`, `-v3`
וקובץ בלי סיומת גרסה, ומוודא ששלושתם נכנסים — היה תופס את הבאג השני
ביום שהוא נכנס. בדיקה ידנית של ה-feed החי: `bkazael.github.io/gamesforum-digest/feed.xml`.

---

## [2026-08-31] - תיקון באג ה-backfill ואימות דו-שלבי לזיהוי כפילויות

### 1. מה עשינו? (What)
* הפרק האמיתי הראשון עם הקוד המשוחזר (31 באוגוסט) יצא קצר משמעותית מהרגיל —
  5 מקורות במקום עד 8, 603 שניות במקום ~700-900. הסיבה נמצאה ב-ledger: שני
  מקומות שהתפנו כתוצאה מזיהוי כפילות **לא התמלאו מחדש** מהמועמד הבא באותו
  מקור.
* `apply_caps()` ו-`dedupe_stories()` (שתי פונקציות נפרדות בקוד המקורי)
  הוחלפו בפונקציה אחת, `select_stories()`: כל מועמד נבדק מול תקרת המקור
  שלו **וגם** מול כפילות באותו מעבר, כך שדחיית כפילות לא "צורכת" מקום
  מהתקרה — המקום פנוי מיד למועמד הבא מאותו מקור.
* זיהוי הכפילות עצמו שופר: הבדיקה המקורית (`_fingerprint`/`_is_name`,
  חפיפת מילים מובחנות) הוכיחה את עצמה כלא אמינה מספיק — באותה ריצה היא
  זיהתה בטעות "Google settles $353m lawsuit" ו-"Google Play new
  performance requirements" ככפילות, כי הן חלקו רק את שם החברה ומילות
  קישור גנריות ("google", "play", "impacting"). עכשיו החוליה הזו היא
  **מסנן מקדים בלבד** (`_heuristic_clash`) — כל התאמה שהיא מסמנת עוברת
  אימות בקריאה קטנה נוספת ל-Gemini (`confirm_same_story`) שבודקת אם מדובר
  *באמת* באותו אירוע חדשותי, לא רק אותה חברה. קריאת האימות רצה רק כשיש
  התאמה חשודה (0-3 פעמים בשבוע לפי נתוני אמת), ונכשלת "פתוח" — שגיאת API
  משאירה את שני הפריטים, לא מוחקת נושא בשקט.

### 2. למה עשינו את זה? (Why)
* המשתמש דיווח על שני נתונים בזמן אמת: פרק קצר, וכפילות שלא היו אמורות
  להיחשב ככפילות. שניהם אומתו מול ה-ledger וקוד הפרק, ושניהם היו תקלות
  אמיתיות בקוד ה-`select()` המקורי (לא משהו שנשבר בשינויי הלילה הקודם) —
  פשוט לא היה עד עכשיו נתון אמיתי שיחשוף אותן.

### 3. איך בודקים?
```
python test_discovery.py
```
בנוי ישירות מהתקלה האמיתית: מוודא שהתנגשות היוריסטית לא נדחית כברירת מחדל
בלי אימות, שדחייה מאושרת באמת משחררת את התקרה למועמד הבא, ושכישלון קריאת
האימות לא מוריד נושא בשקט.

---

## [2026-08-31] - איכלוס זיכרון היסטורי אמיתי (עד 24 באוגוסט)

### 1. מה עשינו? (What)
* הפרק הראשון תחת מערכת הזיכרון החדשה (31 באוגוסט) לא הזכיר שום דבר
  מהפרקים הקודמים — לא כי הפיצ'ר לא עבד, אלא כי `memory.json` לא היה קיים
  כלל לפני הריצה ההיא (נמחק ב-revert), וההקשר נטען *לפני* שהריצה כותבת את
  הרשומה שלה. בעיה נוספת: הפרקים מ-3 עד 24 באוגוסט מעולם לא נכנסו לפורמט
  החדש, ולא היו נכנסים אליו אוטומטית לעולם.
* נקראו בפועל שלושת הפרקים האמיתיים האחרונים לפני ה-31 (15, 17, 24
  באוגוסט) — לא רק הועתק ה-`digest_summary` הגולמי, אלא כל אחד סוכם מחדש
  תוך זיהוי הקווים המחברים בין הפרקים: Mo.co של Supercell חוזר שלושה
  שבועות ברציפות, הוויכוח על עמלת ה-link-out בארה"ב (15 באוגוסט) הופך
  להורדת העמלה באירופה (24 באוגוסט), ו-D2C מעמיק בהדרגה מפרק לפרק.
* `memory.json` עודכן ל-4 רשומות כרונולוגיות (`ep_001`-`ep_004`: 15, 17,
  24, 31 באוגוסט).

### 2. למה עשינו את זה? (Why)
* פיצ'ר הזיכרון קיים בדיוק כדי שהמנחים לא יחזרו על אותם דברים בלי לדעת
  שכבר דיברו עליהם. אכלוס חד-פעמי של ההיסטוריה האמיתית — לא רק הריצה
  הראשונה תחת המערכת החדשה — הוא מה שנותן לפרק הבא (7 בספטמבר) המשכיות
  אמיתית מהיום הראשון, במקום להמתין שבועות עד שהארכיון ייבנה מאפס.

### 3. איך בודקים?
```
python3 -c "import memory; print(memory.load_recent_context())"
```
מציג את 3 הפרקים האחרונים (17, 24, 31 באוגוסט) בדיוק כפי שהם ייכנסו
לפרומפט של הפרק הבא.

---

## [2026-08-24] - שחזור מנוע ה-Discovery העובד

### 1. מה עשינו? (What)
* commit `0c89103` מאותו יום החליף את `discovery.py` העובד (668 שורות: איסוף,
  BLOCK, SIGNAL, SCORE, ledger, דה-דופליקציה, זמן אוויר) בגרסה חדשה בת 188
  שורות שלא ייצאה את `select()` שה-pipeline מייבא, ושמעולם לא באמת אספה
  כתבות (`run_discovery()` רץ על `mock_items` בלבד).
* שוחזר לגרסה שהייתה ב-`9c527d5` (הריצה התקינה האחרונה, לפני 10:00 בבוקר
  שעון סיאטל ב-24 באוגוסט) — `discovery.py`, `sources.py` וה-ledger חזרו
  לעבוד.

### 2. למה עשינו את זה? (Why)
* ה-workflow השבועי נכשל בכל ריצה מאז הרגע הזה (`ImportError` על `select`),
  ובלי ה-revert שום שינוי נוסף לא היה בר-בדיקה מול משהו שבאמת רץ.

### 3. איך בודקים?
```
python test_contracts.py
```
בודק בין השאר ש-`from discovery import select` בכלל מצליח.

---

## [2026-08-31] - איחוד שמות ל-Gemini בלבד, ותיקון סכימת דירוג שקטה

### 1. מה עשינו? (What)
* הוסר ה-alias `claude_json = gemini_json` מ-`gamesforum_pipeline.py`;
  `discovery.py` מייבא `gemini_json` ישירות.
* משתנה הסביבה שנבדק ב-`discovery.py main()` שונה מ-`ANTHROPIC_API_KEY`
  ל-`GEMINI_API_KEY`, והוסר משורת ה-`env` הלא-בשימוש ב-`weekly-digest.yml`.
* **תוקנה שגיאה אמיתית:** ל-`SCORE_SCHEMA` (סכימת הדירוג ב-`discovery.py`)
  היו סוגי שדות ב-lowercase (`"object"`, `"integer"`) — מוסכמת ה-JSON
  Schema הרגילה, לא מה ש-Gemini מצפה לו. `PODCAST_SCHEMA` באותו קובץ כבר
  השתמש נכון ב-UPPERCASE (`"OBJECT"`, `"STRING"`). כל השדות ב-`SCORE_SCHEMA`
  הוסבו ל-UPPERCASE.

### 2. למה עשינו את זה? (Why)
* לא הייתה כאן קריאה כפולה ל-API מעולם — כל הקוד תמיד קרא רק ל-Gemini,
  ה-`claude_json` היה כינוי היסטורי בלבד משלב מוקדם יותר בפרויקט. אין כאן
  שום סיכון לאיכות הפרק.
* ה-casing השגוי ב-`SCORE_SCHEMA` סביר שגרם ל-Gemini לדחות כל בקשת דירוג,
  בשקט: `score_all()` תופס כל שגיאה לכל batch ורושם אזהרה בלבד, כך שכל כתבה
  הייתה נופלת לדירוג החלופי לפי צפיפות נתונים בלבד — בלי שאף אחד ידע.

### 3. איך בודקים?
```
python test_contracts.py
```
כולל בדיקה סטטית (`schema_problems`) שכל `"type"` בכל סכימת Gemini
בקוד הוא ערך תקין, וטסט שמריץ `select()` מקצה לקצה עם mocks ומוודא
שהוא מחזיר בדיוק את המפתחות ש-`generate_podcast_content()` צריך.

---

## [2026-08-31] - חזרה לקצב שבועי, ותקרת ביטחון לגידול עתידי

### 1. מה עשינו? (What)
* `[thresholds].max_age_days` ב-`profile.toml` חזר מ-30 ל-7 — הניסיון
  החד-פעמי בסריקת חודש שלם הסתיים.
* נוספה `[thresholds].max_candidates_to_fetch` (ברירת מחדל: 150) — תקרה על
  כמות המועמדים ש-stage 3 ב-`discovery.py` יוריד בפועל בכל ריצה. כל fetch
  הוא בקשת רשת טורית עם ניסיונות חוזרים; ב-4 מקורות של היום זה לא מגיע
  לקרוב לתקרה, אבל אם יתווספו מקורות נוספים זה בדיוק השלב שיאיים על ה-20
  דקות timeout של GitHub Actions. חריגה מהתקרה נרשמת ב-log וב-ledger
  (`SKIPPED`), לא בשקט.

### 2. למה עשינו את זה? (Why)
* הריצה השבועית היא המצב הקבוע; 30 יום היה ניסוי חד-פעמי.
* `SCORE_BATCH = 20` כבר תומך בכמות מועמדים גדולה יותר בלי שינוי — התקרה
  החדשה מגנה רק על שלב ה-fetch, שהוא הצוואר-בקבוק האמיתי אם מספר המקורות
  יגדל.

### 3. איך בודקים?
בדיקה ידנית: `python discovery.py --dry-run` (ללא עלות טוקנים) ובדיקה
ב-ledger שנוצר שה-`max_age_days` בפועל הוא 7.

---

## [2026-08-31] - זיכרון פרקים: רצף נרטיבי בין פרקים, ללא עלות נוספת

### 1. מה עשינו? (What)
* נוסף `memory.py`: מודול חדש, עצמאי, שנגזר במלואו מה-`digest_summary`
  שה-pipeline כבר מייצר בכל פרק. **אין קריאת Gemini נוספת** — `build_entry()`
  לוקח את הכותרות וה-key_takeaways של הפרק שכבר נוצר וכותב אותם ל-
  `memory.json`.
* `generate_podcast_content()` מקבל פרמטר חדש, `memory_context` (ברירת
  מחדל: מחרוזת ריקה). כשיש היסטוריה, נוסף לפרומפט סעיף אחד — "PREVIOUS
  EPISODES" — עם הנחיה מפורשת להזכיר רק כשרלוונטי ולא לאלץ. שאר הפרומפט
  (STRUCTURE, CHARACTER DYNAMICS, SPEECH NATURALISM) לא השתנה כלל.
* `[memory].lookback_episodes` (ברירת מחדל: 3) ב-`profile.toml` קובע כמה
  פרקים אחרונים נכנסים לפרומפט. הארכיון המלא נשמר ב-`memory.json` לצמיתות
  — רק מה שנכנס לפרומפט מוגבל, כדי שעלות הטוקנים לא תגדל עם הזמן.

### 2. למה עשינו את זה? (Why)
* המטרה: שהפודקאסט יישמע כמו תוכנית מתמשכת, לא שישה פרקים חד-פעמיים —
  בלי לסבך את הפרויקט ובלי לגעת בטון שכבר עובד מצוין.
* ניסיון קודם (24 באוגוסט) לבנות זיכרון דרך `DiscoveryEngine` נזרק לגמרי
  יחד עם ה-revert — הוא מעולם לא היה מחובר לשום דבר שרץ, וה-`memory.json`
  שלו היה ממולא ידנית (עם שאריות `[cite: N]` מהעתק-הדבק). הגרסה הזו
  נכתבת אוטומטית בסוף כל ריצה מוצלחת בלבד — פרק שנכשל לא משאיר רישום שקרי.

### 3. איך בודקים?
```
python test_memory.py
```
מכסה: קובץ חסר, גזירה מ-digest_summary, הגבלת lookback, קריאת
lookback_episodes מ-profile.toml, וקובץ זיכרון פגום שלא מפיל את הריצה.

---

## [2026-08-31] - מערכת בדיקות מדורגת: אמינה בלי לשרוף טוקנים

### 1. מה עשינו? (What)
* **Tier 0/1 — `test.yaml`, רץ על כל push, עלות אפס:** `test_episode.py`
  (קיים) + `test_memory.py` + `test_contracts.py` (חדשים). ללא רשת, ללא
  מפתח API אמיתי.
* **Tier 2 — `manual_test.yaml`, `workflow_dispatch` בלבד, עלות אמיתית
  קטנה:** `live_smoke.py` — כתבת דמה אחת, בקשת תסריט קצרה (`target_words`,
  פרמטר חדש שאף קריאה אמיתית לא משתמשת בו), TTS chunk יחיד. מחליף את
  `sandbox_pipeline.py` שהיה בנוי מול `DiscoveryEngine` שכבר לא קיים ותמיד
  גילה אפס כתבות.
* **Tier 3 — `weekly-digest.yml`:** הריצה האמיתית, ללא שינוי.

### 2. למה עשינו את זה? (Why)
* `test_contracts.py` בונה בדיוק את התפר שנשבר ב-24 באוגוסט: ייבוא
  המודולים יחד, בדיקה סטטית של כל סכימת Gemini, והרצת `select()` מקצה לקצה
  מול נתוני דמה כדי לוודא שהצורה שהוא מחזיר תואמת למה ש-
  `generate_podcast_content()` דורש. בדיקת ייבוא לבדה הייתה תופסת את
  התקלה של 24 באוגוסט ביום שהיא נכנסה.
* גם תוקנה סטייה קיימת: `test_episode.py` בדק chunking מול הגבלה של 1500
  תווים בעוד הקוד בפרודקשן כבר השתמש ב-3800. שני הצדדים מייבאים עכשיו
  קבוע יחיד, `TTS_CHUNK_CHAR_LIMIT`, כדי שזה לא יקרה שוב.

### 3. איך בודקים?
```
python test_episode.py && python test_memory.py && python test_contracts.py
```
כולם רצים בלי `GEMINI_API_KEY` אמיתי. `live_smoke.py` דורש מפתח אמיתי
ומיועד להרצה ידנית בלבד, לפני שינוי מודל/סכימה/פרומפט.
